server:
  port: 80
spring:
  application:
    name: bogeplus-gateway
  cloud:
    nacos:
      discovery:
        #本地开发环境中，这里nacos不能使用云服务器的
        server-addr: localhost
        port: 8848


#      config:
#        server-addr:




